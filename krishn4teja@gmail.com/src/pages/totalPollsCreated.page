<apex:page controller="ChatterHelperUtil">
    <html>
  <head>
    <script type="text/javascript" src="https://www.google.com/jsapi"></script>
    <script type="text/javascript">
      google.load("visualization", "1", {packages:["corechart"]});
      google.setOnLoadCallback(drawChart);
      function drawChart() {
        var data = google.visualization.arrayToDataTable([
          {!linkPostCount }
        ]);

        var options = {
          title: 'Total Links Shared'
        };

        var chart = new google.visualization.PieChart(document.getElementById('chart_div_totalPollsShared'));
        chart.draw(data, options);
      }
    </script>
  </head>
  <body>
    <div id="chart_div_totalPollsShared" style="width: 900px; height: 500px;"></div>
  </body>
</html>  
</apex:page>